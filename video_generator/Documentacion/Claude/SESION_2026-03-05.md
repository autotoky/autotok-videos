# Sesión 2026-03-05

## CONTEXTO OPERATIVO (LEER SIEMPRE TRAS COMPACTAR)
- **Sara** es la tech lead de AutoTok. Habla en español. Código en inglés, UI/mensajes en español.
- **El publisher se ejecuta en la máquina Windows de Sara**, NO desde la VM Linux de Claude.
  - Sara lanza el publisher desde PowerShell y pega los logs aquí.
  - Claude NO puede ejecutar el publisher (no tiene Playwright ni Chrome).
  - Claude edita el código, Sara lo ejecuta, Sara pega logs, Claude diagnostica.
- **Flujo de trabajo**: Claude edita archivos → Sara ejecuta en su PC → Sara pega output/logs → Claude analiza y ajusta.
- **BD**: `video_generator/autotok.db` (SQLite). Claude puede leerla desde la VM (está en el mount).
- **Config**: `video_generator/config_publisher.json`. lotopdevicky usa Profile 8 de Sara para testing.
- **Linear**: Tickets del proyecto AutoTok (equipo QUA). Claude actualiza estados cuando se completan.
- **Hoja Google**: SÍ hay sincronización BD → Google Sheets (Sara confirmó que se actualiza).
- **Sesiones anteriores relevantes**: `Documentacion/Claude/SESION_2026-03-04_parte2.md`

## Resumen
1. Publicación atómica implementada (abort si falla cualquier elemento)
2. Test lote 1: 3/3 videos publicados (12:05, 13:10, 14:15)
3. Test lote 2: 3/5 publicados, 2 magcubic fallaron (ID producto incorrecto en escaparate TikTok)
4. Abort funcionó correctamente: magcubic quedaron "En Calendario" para reintento
5. Textos promo ampliados de 2 a 8 opciones

## Cambio 1: Publicación atómica en `tiktok_publisher.py`

### `_rellenar_descripcion()` — ahora retorna bool
### `_agregar_producto_escaparate()` — ahora retorna bool
### `publicar_video()` — comprueba CADA paso y aborta si falla
- Paso 4 (descripción) → ABORT si falla
- Paso 5 (escaparate) → ABORT si falla
- Paso 5b (etiqueta IA) → ABORT si falla
- Paso 6 (programación) → ABORT si falla (ya existía)
- Paso 7 (confirmar) → ABORT si falla (ya existía)

## Cambio 2: Textos promo ampliados en `config_publisher.json`
8 textos: OFERTON, Sólo hoy!, Últimas unidades, Oferta HOY, Sólo quedan 3, Con descuento HOY, Último día de oferta!, Quedan 4 unidades
Selección: `random.choice()` — aleatorio independiente por video.

## Test lote 1: 3/3 exitosos
Comando: `python tiktok_publisher.py --cuenta lotopdevicky --fecha 2026-03-06 --limite 3`

| Hora  | Producto | Post ID | Estado |
|-------|----------|---------|--------|
| 12:05 | arrancador_coche_EIGOTRAV | 7613706431463705878 | Programado |
| 13:10 | anillo_simson | 7613707020474043670 | Programado |
| 14:15 | MINISO_MS180 | 7613707608481991958 | Programado |

## Test lote 2: 3/5 exitosos, 2 fallidos (abort correcto)
Comando: `python tiktok_publisher.py --cuenta lotopdevicky --fecha 2026-03-06 --limite 5`

| Hora  | Producto | Post ID | Estado |
|-------|----------|---------|--------|
| 15:25 | proyector_magcubic | - | ❌ ABORT → En Calendario (ID producto no encontrado) |
| 16:25 | MINISO_MS180 | 7613710568133840150 | Programado |
| 17:35 | proyector_magcubic | - | ❌ ABORT → En Calendario (ID producto no encontrado) |
| 19:55 | bateria_power_bank_5000 | 7613711916459986198 | Programado |
| 21:55 | bateria_power_bank_5000 | 7613712502001585430 | Programado |

**Causa del fallo magcubic**: El ID 1729677272926690188 (de url_producto en BD) no coincide con el producto que las operadoras añadieron al escaparate de TikTok. Sara va a añadir el producto correcto y relanzar.

## Estado actual calendario lotopdevicky 6-marzo
- 08:00 anillo_simson → Programado
- 09:05 arrancador_coche → Programado
- 10:05 bateria_power_bank_5000 → Descartado
- 11:05 landot_cepillo → Programado
- 12:05 arrancador_coche → Programado
- 13:10 anillo_simson → Programado
- 14:15 MINISO_MS180 → Programado
- 15:25 proyector_magcubic → En Calendario ← pendiente (arreglar escaparate)
- 16:25 MINISO_MS180 → Programado
- 17:35 proyector_magcubic → En Calendario ← pendiente (arreglar escaparate)
- 19:55 bateria_power_bank_5000 → Programado
- 21:55 bateria_power_bank_5000 → Programado

## Notas
- Hoja Google SÍ se sincroniza con BD (Sara lo confirmó, Claude no lo sabía).
- Textos promo nuevos aún no probados (todos los tests usaron "Últimas unidades" del config anterior).
- bateria_power_bank (IA=No) correctamente NO activó toggle IA.

## Instrucción añadida a INSTRUCCIONES_CLAUDE.md
- Regla 5 "Durante la sesión": Log de sesión continuo — actualizar sobre la marcha.

## QUA-82, QUA-83, QUA-84: Deuda técnica limpiada

### QUA-84: Encoding subprocess (Done)
- 6 archivos: añadido `encoding='utf-8', errors='replace'` a subprocess.run()
- Archivos: setup_publisher.py, limpiar_producto.py, cli.py (4 llamadas)

### QUA-82: Caracteres especiales FFmpeg (Done)
- `sanitize_filename()` en generator.py: ampliada con más diacríticos, símbolos, dashes, paréntesis
- `escape_text_for_ffmpeg()` en overlay_manager.py: ampliada + escapado de ; [ ]

### QUA-83: Eliminar except:pass (Done)
- 27 instancias reemplazadas por `log.debug()` contextual
- Archivos: tiktok_publisher.py (23), generar_material.py, lote_manager.py, debug_tiktok_urls.py, cli.py

### QUA-81: Pain test (Done)
- Análisis completado, 47 vulnerabilidades documentadas
- Todos los tickets derivados (82-85) cerrados

## Mensajes de error para operadoras (email)
Añadidas sugerencias accionables para nuevos tipos de error:
- `escaparate_failed`: "Comprueba que el producto está correctamente añadido al escaparate de TikTok Shop"
- `description_failed`: "Puede ser un error temporal de TikTok Studio"
- `ia_label_failed`: "Puede ser un error temporal"

## QUA-43: Pack instalación operadoras

### `scripts/setup_operadora.py` (NUEVO)
Script Python que automatiza la configuración del PC de la operadora:
- Auto-detecta Chrome User Data via `LOCALAPPDATA`
- Lista perfiles Chrome con nombre legible (lee `Preferences` JSON)
- Auto-detecta Google Drive (múltiples candidatos)
- Operadora selecciona: cuenta, perfil Chrome, confirma Drive
- Actualiza `config_publisher.json` con chrome_profile y chrome_profile_name del PC
- Crea `config_operadora.json` con cuenta + drive_path

### `INSTALAR.bat` actualizado
Antes: pedía datos manualmente con `set /p` y generaba JSON con `echo`.
Ahora: instala deps + Playwright, luego lanza `python scripts\setup_operadora.py`.

### Flujo completo operadora:
1. Doble-click `INSTALAR.bat` → instala Python deps + configura PC
2. Doble-click `PUBLICAR.bat` → busca lote pendiente en Drive, publica

### Python embebido (para que operadoras no instalen Python)
- Descargado Python 3.12.7 embebido (Windows embeddable package 64-bit)
- Extraído en `Kevin/python/` junto con `get-pip.py`
- `INSTALAR.bat` reescrito: usa `python\python.exe` local, habilita pip via `.pth`, instala Playwright, lanza setup_operadora.py
- `PUBLICAR.bat` actualizado: usa `python\python.exe` local

### Detección de chrome.exe añadida a setup_operadora.py
- Nueva función `detectar_chrome_exe()`: busca en Program Files, Program Files (x86), y LOCALAPPDATA
- Guarda `chrome_path` en config_publisher.json

### Carpeta renombrada a "Kevin"
- Sara renombró `video_generator/` a `Kevin/` en Drive
- No rompe nada: todos los scripts usan rutas relativas
- "video_generator" solo aparece en documentación (cosmético)

### Regla 6 añadida a INSTRUCCIONES_CLAUDE.md
- Sync a Drive: avisar a Sara cuando se modifiquen archivos que afecten a operadoras

### Test en segundo PC (Sara-Yeast) — INCOMPLETO
- PC: Windows 10 Home, Chrome en `Program Files (x86)`, usuario Sara-Yeast
- INSTALAR.bat ejecutado OK: Python embebido funciona, pip funciona, setup_operadora.py configura cuenta+perfil
- Lote exportado para día 7 (10 videos lotopdevicky) con lote_manager.py
- PUBLICAR.bat: encuentra lote OK, muestra resumen OK, pide confirmación OK
- **PROBLEMA**: Publisher abre Chrome con perfil incorrecto a pesar de config correcta
  - config_publisher.json tiene: chrome_profile=Sara-Yeast, chrome_profile_name=Default, chrome_path=Program Files (x86)
  - AutoTok_Chrome/lotopdevicky borrado para forzar re-copia
  - Sigue abriendo perfil equivocado → pendiente de investigar
- **NOTA**: Playwright Chromium NO se usa. El publisher lanza Chrome real via subprocess + CDP. No hace falta `playwright install chromium`.

### Problema Drive compartido
- Las carpetas en "Elementos compartidos" de Drive NO se sincronizan localmente en Drive desktop
- "Añadir acceso directo a Mi unidad" tampoco funciona en todos los casos
- Para el test Sara descargó la carpeta como zip
- Pendiente decidir modelo de distribución para las operadoras (Drive/servidor/otro)
- Sara sugirió un servidor pequeño como alternativa

## Archivos modificados en segunda parte de sesión
- `INSTALAR.bat` — reescrito para Python embebido + setup_operadora.py
- `PUBLICAR.bat` — actualizado para Python embebido
- `scripts/setup_operadora.py` — añadida detección de chrome.exe + guardar chrome_path
- `Documentacion/Claude/INSTRUCCIONES_CLAUDE.md` — regla 6 (sync a Drive)

## Próximos pasos (para mañana)
1. **URGENTE: Debuggear perfil Chrome en segundo PC** — el publisher abre perfil incorrecto a pesar de config correcta. Investigar cómo `_preparar_perfil_debug()` copia/lanza el perfil.
2. Decidir modelo distribución operadoras (Drive compartido no funciona bien → ¿servidor? ¿otro?)
3. Relanzar 2 magcubic cuando operadoras arreglen el escaparate
4. Restaurar config_publisher.json: lotopdevicky → perfil de Vicky (cuando termine testing)
5. QUA-92: Dashboard HTML
6. QUA-102: Migrar get_connection() legacy
